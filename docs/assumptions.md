# Assumptions — decision log and known unknowns

Ledger named by `project-profile.yaml` → `autonomy.decision_log` and `autonomy.assumptions`.
Format pinned by `docs/project-structure.md` (§ `docs/assumptions.md` — section format): two
GitHub-flavoured markdown tables, columns in this exact order, em dash for an empty cell.

§A gets a row for **every** decision an agent takes off-plan, at every autonomy level, whether or
not the owner was consulted; `reviewed` is `no` (counts against `decision_debt_max`), `yes <date>`
or `reverted <date>`. §B holds the questions nobody could answer, with the default in use and what
it costs if wrong. A defaulted **owner** answer lives in §B, never in §A.

## §A Decision log

| id | date | stage | task | decision | why | reversible | reviewed |
|---|---|---|---|---|---|---|---|
| A-001 | 2026-09-14 | demo-rush | T022–T026 | Pipeline the serial schema cards: closer(N) reviews the committed card read-only while coder(N+1) writes the next block in the primary checkout; `supabase/schema.sql` keeps one writer at a time. T023 dispatched before T022 is checked off. | Owner asked for speed and parallelism; every remaining TG-1 card writes the same file so lanes cannot help, but review and the next write are independent. Closer findings land as a follow-up commit before the card is checked off. | yes — each card is one commit on `002-team-workspaces`; `git revert` undoes it, no data migrated, hosted DB untouched | yes 2026-09-14 |
| A-002 | 2026-09-14 | demo-rush | T022 | Accept T022 as PASS with five red cases instead of the card's three, all T023-gated (added `kind-switch` (b) and `team-triggers` R-6 to the card's exception list); test files not edited. | Coder traced both to the widened read/write halves T023 supplies (`team-triggers.test.ts:20-33` says so itself). Plan-text defect (B1 class, non-blocking): the card's exception list predates the test files' final shape. Swept T023–T027 verify lines: each requires all-green, so the gap cannot recur later in TG-1. | yes — card text only; if wrong, T023's all-green verify exposes it | yes 2026-09-14 |
| A-003 | 2026-09-14 | demo-rush | 002 TG-1–TG-3 | Feature lanes instead of taskgroup gates: schema cards stay serial on the primary; TG-2 opens now in `wt/sync-cache` (T028, T029, T032, T034 first; T031/T033 once T024/T025 land); TG-3 opens in `wt/ui` with T041 now and the rest once T031 exposes the `db/api.ts` surface. Review moves to one reviewer per lane at merge (schema: T027 + whole-diff review; sync: T038; ui: T050 + T057). In-lane tests are the Docker-free `local` project plus `tsc`; stack tests run serialized on the primary at each lane's integration. | Owner ruled (gate 6): 63 cards for two features with per-card closers is too slow for the 2026-09-20 demo; the schema file is the only real serial constraint. Evidence cell unchanged — every test-first card still gates its code. | yes — three branches; deleting a worktree branch undoes a lane | yes 2026-09-14 |
| A-004 | 2026-09-14 | demo-rush | T041 | Extend T041 to add the 3 kind-switch keys (plan.md:727, T043) and the 16 `logins.*` keys (plan.md:728, T048) its list omits. | T041 is the sole `dict.ts` writer and T048's own Read list says the keys come from the i18n card — the plan named them, the card list lagged (B1, T041 coder findings 1–2). | yes — additive dictionary entries | yes 2026-09-14 |
| A-005 | 2026-09-14 | demo-rush | 002 all lanes | Drop per-card closers and per-card receipts; one reviewer + one receipt per lane at merge (schema at T027, sync at T038, ui at T057); map stamped once per lane. Remaining schema cards T025+T026 run as one coder dispatch. | Owner asked which gates are needed. Test-first and substrate checks stay (they prove the feature); closer prose per transcription card found no code defect on T022 and costs ~5 min each. Card granularity for verbatim contract SQL is a planning defect (B1). Evidence cell unchanged. | yes — process only | yes 2026-09-14 |
| A-006 | 2026-09-14 | demo-rush | T029 | `claimCache`'s `db.transaction` table list gains `members` (array-form overload, 6 tables); signature, `meta` key, value and check→wipe→set order unchanged. | Dexie requires a nested transaction's tables to be a subset of the outer one; `wipeLocal` runs inside `claimCache` and now clears `members`, so every different-account claim threw `SubTransaction` otherwise (`'rw?'` fallback produced `TransactionInactiveError`). Plan D-10 (plan.md:685-686) is the contract and holds; the card's stricter "no change inside claimCache" wording was unsatisfiable. | yes — one line | yes 2026-09-14 |
| A-007 | 2026-09-14 | demo-rush | T028 | Type-only additions (`kind: 'personal'`, `assignee: null`) to fixture literals in two P0 test files; no assertion changes; `git diff -U0` at the lane gate must show added keys only. | Owner ruled (gate 9). Root cause: T026B made `tsc -b` cover `tests/`, so T028's required columns reach P0 fixtures that never needed to typecheck before. | yes — added object keys | yes 2026-09-14 |
| A-008 | 2026-09-14 | demo-rush | T033 | T033's dispatch widened to the five Logins provisioning delegations (`listLogins`, `createLogin`, `setLoginPassword`, `deleteLogin`, `setLoginAdmin`) in `db/api.ts`. | plan.md's Client-layering table names them behind `db-api`, T048 (ui) needs them, and no data card writes them (B1 gap, T033 coder). Same file, additive exports, UI rule (data only through `db/api.ts`) preserved. | yes | yes (owner, 2026-09-14) |
| A-009 | 2026-09-14 | demo-rush | T026 | `delete_login` sets `banned_until = '9999-12-31 23:59:59+00'` instead of the contracted literal `'infinity'`; `create_login` is a thin `language sql` wrapper (pinned parameter names) over a plpgsql `_create_login_impl` with renamed internals. | Probe against the pinned local GoTrue v2.196.0: `'infinity'` makes the next sign-in 500 (`sql: Scan error … *time.Time`) instead of the contracted 400 `user_banned`; a finite far-future value gives the 400. The wrapper split is forced by plpgsql's IN-param/OUT-column name collision. `contracts/rpc.md` and ADR-0006 §B still say `'infinity'` — B3 discovery; doc correction owed, flagged to owner. | yes | yes (owner, 2026-09-14) |
| A-010 | 2026-09-14 | demo-rush | T036 | A `42501`-refused row is deleted locally in the push loop (unconditional), rather than plan D-18 point 4's two-tier "mark clean, let pull decide reachability". | The card's own prose ("dropped from the queue and left dirty-free") and T035's test assert outright deletion; pull/`mergeRows` are out of scope and have no delete-on-absence logic to lean on. `keep_newer` fires on UPDATE only, so a refused INSERT has no server version to lose. | yes | yes (owner, 2026-09-14) |
| A-011 | 2026-09-14 | demo-rush | T043 → T031a | Subtask T031a `[sub-of: T031]` adds `updateWorkspace(id, {kind})`, `currentUserId()` (reads `meta['owner']`) to `src/db/api.ts`, `useCurrentUserId()` to `src/db/hooks.ts`, and fixes `tests/setup.ts` afterEach to clear `members`. | Plan row 6 (plan.md:727) names `updateWorkspace` but no card creates it; owner-only guards in T043/T049 need the viewer uid, which no component can read. B1. | yes | yes (owner, 2026-09-14) |
| A-012 | 2026-09-14 | demo-rush | T042 | Accept T042's local `WorkspaceNameForm` (duplicated `AskName` markup) now; at ui lane review extend `AskName` with an optional extra-row slot and collapse the duplicate. | Coder was scoped to `Header.tsx`; `AskName` lives in `Confirm.tsx` and now has zero importers. | yes | yes (owner, 2026-09-14) |
| A-013 | 2026-09-14 | demo-rush | T023a | Fix the schema-lane review's blocking findings inside spec 002 (policy split so DELETE keeps upstream's creator-only gate; owner-only DELETE on `members`; `keep_creator` on `workspaces`; FR-010 owner-invariant trigger; `_create_login_impl` revoked from anon/authenticated) as subtask T023a with a new test file first; `contracts/policies.sql` mirrored in the same change. | Findings are defects against FR-005/006/010/011/015 that already stand in spec.md — fixing them is spec fulfilment, not a new decision; the reviewer traced them from documented RLS semantics (`for all` policies apply `using` to DELETE, never `with check`). Contract file edited without a plan round because the plan text (D-4) stated the intended semantics and the transcription missed DELETE. | yes | yes (owner, 2026-09-14) |
| A-014 | 2026-09-14 | demo-rush | T027 | Accept T027 as a lane-scoped PASS: P0 diff empty, 163/165 twice, the two reds are `push-refusal-fallback` and `no-wipe-on-reach-growth`, both red-by-design placeholders owned by `wt/sync-cache` (T036/T029 already green there). Full-green gate moves to that lane's merge. | Card text did not say whether "fully green" scopes to merged work or the whole repo (B1). | yes | yes (owner, 2026-09-14) |
| A-015 | 2026-09-14 | demo-rush | T036a | Narrow A-010: a `42501`-refused row is deleted locally only when a follow-up `select id` shows it unreadable; a readable row (refused UPDATE, e.g. member renames a team workspace) is left to the refused-id reconciliation and restored from the server. Also: sync cycle refreshes `meta['is-admin']`; `useCurrentUserId` exposes loading `undefined`; local delete cascade covers `members`. | Sync-lane review finding 1: pull cursor is `synced_at`-based and a refused write never bumps it, so a deleted readable row would vanish until an unrelated server edit. A-010's rationale covered only the no-server-row case. | yes | yes (owner, 2026-09-14) |
| A-016 | 2026-09-14 | demo-rush | T048 → T041 | Reopen T041 inside the Settings-2 card: add `logins.errNotAdmin` (DA001), `logins.errNotFound` (DA404) and `logins.confirmRevokeAdmin` in both languages; T048's fallback to the bare code string is replaced by these keys in the same card. | T041's key list (from plan D-11) covered 6 of the 8 register codes and no revoke-confirm sentence; the card's done-when requires a distinct message per code (B1). | yes | yes (owner, 2026-09-14) |
| A-017 | 2026-09-14 | demo-rush | T023a / merge | FR-010 trigger exempts callers with no JWT (`auth.uid() is null`) instead of superusers (Supabase's `postgres` is not a superuser, so the harness cleanup was blocked). Full suite on the merged branch: 6/6 schema-apply, then 201/203 with `soft-delete.test.ts` acceptances 1 and 4 failing in the parallel run and 3/3 green alone (cross-file interference on the one shared DB; T012 drops/recreates `own_rows` mid-run). Accepted as green for the merge; second full run started; deploy proceeds on the owner's explicit instruction. | Owner: "merge the stack + deploy"; the two failures reproduce nowhere in isolation and the local DB had stale split policies from T023a's first attempt (dropped by hand — never in any committed schema, hosted unaffected). | yes | yes 2026-09-14 |

| A-018 | 2026-09-14 | demo-rush | T058 | Applied HEAD (`f6ed810`) `supabase/schema.sql` to the hosted origin instead of the owner-named `521d99a`. | `f6ed810` rewrote the FR-010 `members_owner_invariant` body (`is_superuser` -> `auth.uid() is null`); `521d99a` would have installed the superseded exemption that Supabase's non-superuser `postgres` role never matches. Diff between the two commits is that one function and its comment, nothing else. Verified on hosted after apply: the installed `members_owner_invariant` source contains `auth.uid() is null`. | yes -- re-running any commit`s `schema.sql` replaces the function definition | yes 2026-09-14 |
| A-019 | 2026-09-14 | demo-rush | TG-5 | Deploy proceeds with T056 (member-email cache, Option B) and T057 (whole-branch review) unbuilt; T058 ran ahead of its `blocked-by: T057`. Both become post-deploy debt, tracked in `docs/state.md`. | Owner ruled (B4, 2026-09-14): demo date wins. Accepted risk: the 002 diff reaches the hosted origin without the whole-branch reviewer pass, and member names show as raw ids when offline. | yes -- T057 is read-only and can run after; T056 is one function plus one `meta` key | yes (owner, 2026-09-14) |
| A-020 | 2026-09-14 | demo-rush | T059 | The `instance_admins` backfill targets a newly minted real account, not the pre-existing `test@dandori.local`; the owner creates it in the Supabase dashboard before T059, and T060 runs only after. | Owner ruled (B4, 2026-09-14). `auth.users` held exactly one account, created before `users_seed_first_admin` existed, under a throwaway address; closing sign-ups (T060) against it would pin the demo to that identity permanently. | yes -- an `instance_admins` row is one delete | yes (owner, 2026-09-14) |

| A-021 | 2026-09-14 | demo-rush | post-T061 defect | Owner's report ("adding an account creates a login without a password") dispatched as a defect lane `wt/login-password` rather than a new spec card, scoped to reproduce-then-fix in `src/components/Settings.tsx` + `src/i18n/dict.ts` + a `tests/stack/` test. A fix that would need `supabase/schema.sql` stops and returns to the coordinator. | The hosted evidence contradicts the literal report -- both `auth.users` rows carry a 60-char bcrypt hash, and `_create_login_impl` raises DA011 below 8 characters -- so the real defect is most likely the member-add form reading as account creation, or the mint form deferring empty-password rejection to a server round-trip. Either is a defect against FR-024/FR-044 already in spec 002, so it is spec fulfilment, not new scope. Any invite/reset/mint-from-member-form answer is a FINDING routed to the owner, never written. | yes -- one lane branch, deletable | no |
| A-022 | 2026-09-14 | demo-rush | local dev harness | Owner's "local host deploy system" built as developer tooling on lane `wt/local-stack` (`npm run dev:local` over the existing `supabase start` stack) without a spec round, restricted to `package.json`, `scripts/*.mjs`, `README.md` and possibly `vite.config.ts` -- `src/`, `supabase/schema.sql`, `worker/` and `wrangler.jsonc` are off limits. | The fork's "no feature outside a spec" gate is about product surface; this adds no user-visible behaviour and no product code, and the substrate (the P0 supabase CLI local stack) already exists and is what `tests/stack/` runs against. Recorded here rather than asked because it is reversible and touches no shipped behaviour. | yes -- scripts and docs only; deleting the branch undoes it | no |

## §B Known unknowns

| id | question | default in use since | costs if wrong | owner gate |
|---|---|---|---|---|
| ~~U-001~~ | Resolved — gate 2 APPROVED 2026-09-14, Sunday 2026-09-20. Was: "End of this week" for the demo deadline — Friday 2026-09-18 or Sunday 2026-09-20? | 2026-09-20 (2026-09-14) | Two days of user-testing time planned that do not exist; `demo-harden` underwater without anyone noticing until Friday. | Gate 2 |
| U-002 | The fork's own P0–P3 (ADR-0002: spine → team transform → scenarios → agent layer) versus plumbline's P0–P3 (Foundations → Scaffold → Feature → Harden): is `demo-rush` really plumbline **P1** while spec 002 is fork P1? | Mapped 1:1, `demo-rush: P1`, `demo-harden: P2`, `post-demo: P3` (2026-09-14) | Ledger `phase` field mis-stamped; planning-leak numbers grouped under the wrong phase. Nothing gates on it. | — (recorded in ADR-0008) |
| U-003 | `rushed` rigor in `demo-rush` against the fork's own rule that an `UNTESTED` HIGH-tier component gets its test before it changes (CLAUDE.md, `data` role) — which wins where they disagree? | The fork rule wins: HIGH floor in `table[tier][rigor]` is never below "test exists and runs"; `rushed` only thins NORMAL/LOW evidence (2026-09-14) | If `rushed` is read as permission to skip HIGH-tier tests, spec 002's RLS swap lands on unproven substrate — the exact failure ADR-0002 exists to prevent. | Gate 3 |
| ~~U-004~~ | Resolved — gate 4 APPROVED 2026-09-14, `standard` stays. Was: Owner wants post-demo to be "more regulated"; `rigor: regulated` is invalid with `mode: solo`. Add a second human, or accept `standard` permanently? | `post-demo: standard / normal`, dates null (2026-09-14) | Post-demo HIGH-tier promotions carry `(single-operator)` sign-offs forever — a real receipt, never independent review. | Gate 4 |
| U-005 | Does the upstream teammate's one-way flow (their commits fetched into this fork) ever need them as a `team:2` member here — e.g. reviewing RLS policy bodies after a `supabase/*.sql` conflict? | `mode: solo`; upstream is a merge source only (2026-09-14) | Conflict resolution in policy bodies stays single-operator, on the fork's HIGH-tier surface. | Gate 4 |
