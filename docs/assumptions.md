# Assumptions — decision log and known unknowns

Ledger named by `project-profile.yaml` → `autonomy.decision_log` and `autonomy.assumptions`.
Format pinned by `docs/project-structure.md` (§ `docs/assumptions.md` — section format): two
GitHub-flavoured markdown tables, columns in this exact order, em dash for an empty cell.

§A gets a row for **every** decision an agent takes off-plan, at every autonomy level, whether or
not the owner was consulted; `reviewed` is `no` (counts against `decision_debt_max`), `yes <date>`
or `reverted <date>`. §B holds the questions nobody could answer, with the default in use and what
it costs if wrong. A defaulted **owner** answer lives in §B, never in §A.

## §A Decision log

| id | date | stage | task | decision | why | reversible | reviewed |
|---|---|---|---|---|---|---|---|
| A-001 | 2026-09-14 | demo-rush | T022–T026 | Pipeline the serial schema cards: closer(N) reviews the committed card read-only while coder(N+1) writes the next block in the primary checkout; `supabase/schema.sql` keeps one writer at a time. T023 dispatched before T022 is checked off. | Owner asked for speed and parallelism; every remaining TG-1 card writes the same file so lanes cannot help, but review and the next write are independent. Closer findings land as a follow-up commit before the card is checked off. | yes — each card is one commit on `002-team-workspaces`; `git revert` undoes it, no data migrated, hosted DB untouched | no |
| A-002 | 2026-09-14 | demo-rush | T022 | Accept T022 as PASS with five red cases instead of the card's three, all T023-gated (added `kind-switch` (b) and `team-triggers` R-6 to the card's exception list); test files not edited. | Coder traced both to the widened read/write halves T023 supplies (`team-triggers.test.ts:20-33` says so itself). Plan-text defect (B1 class, non-blocking): the card's exception list predates the test files' final shape. Swept T023–T027 verify lines: each requires all-green, so the gap cannot recur later in TG-1. | yes — card text only; if wrong, T023's all-green verify exposes it | no |
| A-003 | 2026-09-14 | demo-rush | 002 TG-1–TG-3 | Feature lanes instead of taskgroup gates: schema cards stay serial on the primary; TG-2 opens now in `wt/sync-cache` (T028, T029, T032, T034 first; T031/T033 once T024/T025 land); TG-3 opens in `wt/ui` with T041 now and the rest once T031 exposes the `db/api.ts` surface. Review moves to one reviewer per lane at merge (schema: T027 + whole-diff review; sync: T038; ui: T050 + T057). In-lane tests are the Docker-free `local` project plus `tsc`; stack tests run serialized on the primary at each lane's integration. | Owner ruled (gate 6): 63 cards for two features with per-card closers is too slow for the 2026-09-20 demo; the schema file is the only real serial constraint. Evidence cell unchanged — every test-first card still gates its code. | yes — three branches; deleting a worktree branch undoes a lane | yes 2026-09-14 |
| A-004 | 2026-09-14 | demo-rush | T041 | Extend T041 to add the 3 kind-switch keys (plan.md:727, T043) and the 16 `logins.*` keys (plan.md:728, T048) its list omits. | T041 is the sole `dict.ts` writer and T048's own Read list says the keys come from the i18n card — the plan named them, the card list lagged (B1, T041 coder findings 1–2). | yes — additive dictionary entries | no |
| A-005 | 2026-09-14 | demo-rush | 002 all lanes | Drop per-card closers and per-card receipts; one reviewer + one receipt per lane at merge (schema at T027, sync at T038, ui at T057); map stamped once per lane. Remaining schema cards T025+T026 run as one coder dispatch. | Owner asked which gates are needed. Test-first and substrate checks stay (they prove the feature); closer prose per transcription card found no code defect on T022 and costs ~5 min each. Card granularity for verbatim contract SQL is a planning defect (B1). Evidence cell unchanged. | yes — process only | yes 2026-09-14 |
| A-006 | 2026-09-14 | demo-rush | T029 | `claimCache`'s `db.transaction` table list gains `members` (array-form overload, 6 tables); signature, `meta` key, value and check→wipe→set order unchanged. | Dexie requires a nested transaction's tables to be a subset of the outer one; `wipeLocal` runs inside `claimCache` and now clears `members`, so every different-account claim threw `SubTransaction` otherwise (`'rw?'` fallback produced `TransactionInactiveError`). Plan D-10 (plan.md:685-686) is the contract and holds; the card's stricter "no change inside claimCache" wording was unsatisfiable. | yes — one line | no |
| A-007 | 2026-09-14 | demo-rush | T028 | Type-only additions (`kind: 'personal'`, `assignee: null`) to fixture literals in two P0 test files; no assertion changes; `git diff -U0` at the lane gate must show added keys only. | Owner ruled (gate 9). Root cause: T026B made `tsc -b` cover `tests/`, so T028's required columns reach P0 fixtures that never needed to typecheck before. | yes — added object keys | yes 2026-09-14 |
| A-008 | 2026-09-14 | demo-rush | T033 | T033's dispatch widened to the five Logins provisioning delegations (`listLogins`, `createLogin`, `setLoginPassword`, `deleteLogin`, `setLoginAdmin`) in `db/api.ts`. | plan.md's Client-layering table names them behind `db-api`, T048 (ui) needs them, and no data card writes them (B1 gap, T033 coder). Same file, additive exports, UI rule (data only through `db/api.ts`) preserved. | yes | no |

## §B Known unknowns

| id | question | default in use since | costs if wrong | owner gate |
|---|---|---|---|---|
| ~~U-001~~ | Resolved — gate 2 APPROVED 2026-09-14, Sunday 2026-09-20. Was: "End of this week" for the demo deadline — Friday 2026-09-18 or Sunday 2026-09-20? | 2026-09-20 (2026-09-14) | Two days of user-testing time planned that do not exist; `demo-harden` underwater without anyone noticing until Friday. | Gate 2 |
| U-002 | The fork's own P0–P3 (ADR-0002: spine → team transform → scenarios → agent layer) versus plumbline's P0–P3 (Foundations → Scaffold → Feature → Harden): is `demo-rush` really plumbline **P1** while spec 002 is fork P1? | Mapped 1:1, `demo-rush: P1`, `demo-harden: P2`, `post-demo: P3` (2026-09-14) | Ledger `phase` field mis-stamped; planning-leak numbers grouped under the wrong phase. Nothing gates on it. | — (recorded in ADR-0008) |
| U-003 | `rushed` rigor in `demo-rush` against the fork's own rule that an `UNTESTED` HIGH-tier component gets its test before it changes (CLAUDE.md, `data` role) — which wins where they disagree? | The fork rule wins: HIGH floor in `table[tier][rigor]` is never below "test exists and runs"; `rushed` only thins NORMAL/LOW evidence (2026-09-14) | If `rushed` is read as permission to skip HIGH-tier tests, spec 002's RLS swap lands on unproven substrate — the exact failure ADR-0002 exists to prevent. | Gate 3 |
| ~~U-004~~ | Resolved — gate 4 APPROVED 2026-09-14, `standard` stays. Was: Owner wants post-demo to be "more regulated"; `rigor: regulated` is invalid with `mode: solo`. Add a second human, or accept `standard` permanently? | `post-demo: standard / normal`, dates null (2026-09-14) | Post-demo HIGH-tier promotions carry `(single-operator)` sign-offs forever — a real receipt, never independent review. | Gate 4 |
| U-005 | Does the upstream teammate's one-way flow (their commits fetched into this fork) ever need them as a `team:2` member here — e.g. reviewing RLS policy bodies after a `supabase/*.sql` conflict? | `mode: solo`; upstream is a merge source only (2026-09-14) | Conflict resolution in policy bodies stays single-operator, on the fork's HIGH-tier surface. | Gate 4 |
